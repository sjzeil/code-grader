build:
  studentFiles:
    include: ["*.cpp", "*.h"]
    exclude: ["makefile"]
  instructorFiles:
    include: ["makefile"]
    exclude: ["*.cpp", "*.h"]
test:
  launch: ./dividers
  timelimit: 5
  stderr: 0
  grading:
    - oracle: edu.odu.cs.oracle1
  